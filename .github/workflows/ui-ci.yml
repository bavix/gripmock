name: ui ci

on:
  pull_request:
    paths:
      - "ui/**"
      - ".github/workflows/ui-ci.yml"
  push:
    branches: [master]
    paths:
      - "ui/**"
      - ".github/workflows/ui-ci.yml"

permissions:
  contents: read

jobs:
  ui:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Install
        run: npm ci --ignore-scripts
      - name: Type-check
        run: npm run type-check
      - name: Lint
        run: npm run lint:ci
      - name: Unit tests
        run: npm test
      - name: Build
        run: npm run build
      - name: Verify committed dist matches build
        run: |
          if [ -n "$(git status --porcelain -- dist)" ]; then
            git status -- dist
            echo "ui/dist is stale or hand-edited: run 'cd ui && npm run build' and commit the result"
            exit 1
          fi
